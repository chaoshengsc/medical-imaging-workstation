# UI 多角色审查

日期：2026-09-09。初次审查范围是基于HEAD `6619cf5edec75ea1c831f243a9b438ba81064a3d` 的未提交工作区，不是该commit本身的UI评测。第一轮已验收并本地提交，第二轮实施状态如下。既有数据和实验产物未改动。

## 当前增量：帮助目录、段落检索与图解已扩展，待用户查看

2026-09-09：用户查看首版后确认内容覆盖、直观讲解与查阅体验均需改进，并批准本轮扩展。`help_topics.py`作为双语内容和纯Python搜索的唯一来源，覆盖开始阅片、窗宽窗位、多平面与投影、测量与ROI、标注与工作图层、AI与结果导出、序列与对比、工程保存与来源、重建实验室共9主题/30小节。内容作者核对现有操作、参数与不可用前提，专家独立复核来源、AI统计/显示分离、工程不打包DICOM和重建视图角色。

`help_center.py`扩展为按任务分组的目录、全文结果摘要与小节定位、相关主题、前进/返回以及主动“查看当前功能”。输入搜索不自动换文；点击或按Enter才导航。返回保留展开、滚动和教学示例参数。窄于740px时目录与正文分开显示；首屏仍为用途和简短操作，详细内容折叠。新增隔离的窗宽窗位交互映射示意、FBP四窗角色图、原始AI只读显示与工作结果/统计分叉图。示例不读取影像、不触发主窗参数/图层/算法变化。

当前功能定位读取已选任务页、工具、主窗保留的焦点控件；关闭后重开定位，已开窗口仅唤回，不随主窗点击自动跳页。没有全局点击拦截模式，没有自动弹出/自动加载数据/自动实验。旧进程不会热加载代码。主代理负责阅读器/导航/搜索/测试/记录，help_novice负责内容，help_expert只读审计；旧第四轮与六项修复及首版未提交改动保留。本轮未commit、未push、未运行昂贵AI推理。

| 验证 | 结果与边界 |
|---|---|
| 帮助定向回归 | `Annotation_Projects/help-expanded-20260909/targeted.log`：46/46，exit 0。原20项保留并适配目录/段落检索，新增26项覆盖双语搜索、断链、真实匹配摘要、导航历史、概览定位、键盘结果激活、窄窗重开、当前功能和图解隔离。 |
| 最终原生Cocoa | `native.log`：115/115，exit 0；包含上述46项及六项全局修复、框架与切换连续性检查。 |
| 最终隔离数据无关层 | `SKIP_REAL_DATA=1 python tests/test_gui.py`：1487/1487，exit 0，`subset.log`。复制当前跟踪文件与两个新增帮助模块到临时git索引目录；`candidate-tree.txt`标记候选树，不含本节随后追加的记录。不是原工作区真实数据全套或remote CI。 |
| 已知坏对照 | `known-bad.log`：仅测试进程恢复搜索概览的旧提前返回路径；18项中“概览定位顶部”“概览返回原位置”两项准确失败，exit 1，产品磁盘未回退。 |
| 原生窗口与视觉 | 中/英×9主题×940/430px共36组合实际宽度符合请求，正文横向滚动范围均为0，`visual-check.log`。人工查看`overview.png`、`window.png`、`search.png`、`recon-narrow.png`及中英layers截图，确认深色、换行、窄窗图解和分叉说明。无横向溢出不代表系统大字体/屏幕阅读器验收。 |
| 静态和打包 | `ruff check .`、`git diff --check`通过；新模块登记到打包清单，26顶层模块/16 Qt-free模块的inventory回归通过。`evidence.json`绑定实现/测试与日志SHA-256。 |

审查期间已修复：同页搜索概览不回顶部、窄窗搜索后重开只见目录，以及Cocoa列表Enter未发激活信号。初次帮助测试中旧“两个主题筛选”断言随新交互改为小节结果断言；随后新回归真实抓到常见问题短语缺失及列表Enter导航失败，补齐文案与键盘路径后通过。临时原生runner首次把无Qt搜索测试误传app参数，入口报TypeError、无有效检查；修正runner调用后才计上述原生结果。

限制：本轮没有重跑真实MRI/真实数据全套，未量化教学效果、所有辅助技术或全部功能状态。帮助是经源码核对的操作/方法说明，不代替算法验证和研究证据；第一批图解覆盖3类概念，不声称所有功能均已有图示。

### 首版历史：帮助与学习两个主题

2026-09-09：用户确认教学/科研场景、默认不干扰熟练用户，以及“窗宽窗位／重建实验室”两主题方案后实施。新增 `help_center.py`，在已有右侧顶部行增加帮助入口和主窗 F1；默认延迟创建，独立非模态窗口，无画布重排。支持中英文、正文搜索筛选主题、原理/手动练习/方法前提折叠阅读。已打开窗口不随功能切换跳页；关闭后重开才定位当前功能。当前仅覆盖两个入门主题，不称完整说明书。原有第四轮和六项修复未提交改动保留；本轮未commit、未push、未运行昂贵AI推理。

教学练习核对实际代码：重建角度范围与采样密度分开解释；由于弦图生成可消费上一张重建结果，比较采样密度前明确卸载/重载模体以固定来源。练习仅提供静态操作说明，不自动加载、计算、保存或恢复现场；标定状态仍以主界面实际数据为准。打包清单同步登记新增模块，架构文档计数更新为25个顶层模块。

独立专家只读审计实测发现两项P2：搜索Enter触发QDialog默认按钮导致阅读重置、原生Cocoa按Esc关闭帮助后主窗失焦。已关闭主题按钮autoDefault、保留重复主题阅读状态，并在退出帮助时恢复可见主窗焦点（主窗退出除外），补实际键盘回归。视觉检查另外修正帮助滚动内容继承系统白底导致低对比的问题。

| 验证 | 结果与边界 |
|---|---|
| 原生Qt Cocoa | `Annotation_Projects/help-ui-20260909/native.log`：89/89，exit 0；包含新增帮助20项、六项全局修复24项及框架/切换连续性检查。帮助覆盖搜索Enter、Esc焦点、隐藏面板F1、Space/翻页/Ctrl+Z不传播主窗、画布camera/切片/工具不变、关闭生命周期。 |
| 隔离数据无关回归 | `SKIP_REAL_DATA=1 python tests/test_gui.py`：1461/1461，exit 0，`subset.log`。由当前跟踪文件与新增帮助模块复制到临时git索引目录执行，无本机研究目录/外部权重；不是原工作区真实数据全套或remote CI。 |
| 首次隔离失败 | `subset-first.log`：1450项中3项失败；1项因临时副本未初始化git导致跟踪文件检查失败，2项因新增模块尚未登记打包清单。补齐后最终回归通过，未放宽检查。 |
| 原生视觉 | `help.png`（620×650逻辑尺寸）与`recon-narrow.png`（430×560），检查深色背景、选中态、正文换行及展开后滚动。图片为2×屏幕像素。 |
| 静态 | `ruff check .`、`git diff --check`通过。`evidence.json`记录实现/测试源码SHA-256与日志摘要。 |

限制：未做屏幕阅读器与系统大字体全链路验收；不宣称教学效果经过评估。现有程序进程不会热加载此次代码。说明结构参考 [Slicer入门](https://slicer.readthedocs.io/en/latest/user_guide/getting_started.html)、[ImageJ用户指南](https://imagej.net/learn/user-guides)和[Diátaxis](https://diataxis.fr/)，外部文档不作为本项目算法/测量有效性证据。

## 当前状态：第四轮保留，六项全局问题已修复并本地验证，均待用户验收/提交

2026-09-09：用户明确授权修复下列六项问题，保留第四轮未提交改动，先复现、补回归、修复，再跑原生Qt及数据无关回归。本轮未commit、未push，未运行昂贵AI推理，未改变模型/算法/数据。历史审查的“尚未修复”属于当时快照，当前状态以本节为准。

- 对比退出统一恢复V1–V4临床标题并清旧差值显示；进入对比隐藏renderer不采用的临床方向、投影、独立窗位/显示控件，保留原选项供返回恢复，仍沿用第四轮的固定工具条高度。
- 对比保存临床source对象/Series UID、各窗camera、MPR和平面、分隔比例；进入使用独立适配，返回仅对有效来源恢复。MPR通过QSignalBlocker恢复，避免触发默认平面重排；来源对象或UID变化不会恢复旧camera/MPR。
- 离线工程接入撤下旧AI状态、器官图例、统计/CSV/三维按钮及来源能力；工程图层/标注本身保留。独立模体仍在时重绘V1，返回临床仍显示匹配来源连接引导。
- Zoom在绘制时从实际transform取值，覆盖滚轮、布局适配和相机恢复；仅刷新显示文字，不为它重算整幅影像。

新增`test_global_ui_state_fixes`进入数据无关层，24项检查通过实际对比入口、真实临时.miwproj读写、合成AI完成回调与Qt滚轮覆盖上述六类。实现由视觉代理负责`compare_lab.py`、工作流代理负责`annotation_lab.py`，主代理负责`main.py`/`graphics_view.py`、集成回归与本记录；第四轮`ui_builder.py`/`recon_lab.py`修改完整保留。

| 验证 | 结果与证据 |
|---|---|
| 修改前已有probe复现 | `ui-global-audit-20260909/compare-pre-fix-round5.json`、`offline_ui_repair_before.json`另存复现，未覆盖原baseline；Zoom复现另存`ui-six-fixes-20260909/main_probe_before.log/json`。 |
| 定向回归 | `ui-six-fixes-20260909/targeted.log`：24/24，exit 0。 |
| 已知坏对照 | `ui-six-fixes-20260909/known-bad-final.log/json`：仅进程内恢复旧对比/离线/控件/Zoom实现，24项中15项失败、exit 1，覆盖六类旧行为；产品磁盘未回退。首次AST提取旧绘制方法的对照进程exit 139，无有效断言结果；修正提取后父类绘制调用再得到上述可解释失败，保留原`known-bad.log`，不把该崩溃当作产品回归证据。 |
| 最终原生Cocoa | `ui-six-fixes-20260909/native.log/json`：139/139，exit 0；包括新24项及第四轮框架/空态、前三轮状态/窄窗、序列对比、公开RIDER CT三面编辑保存Undo。JSON绑定最终7个源码/测试文件SHA-256。 |
| 最终隔离数据无关层 | `SKIP_REAL_DATA=1 python tests/test_gui.py`：1441/1441，exit 0；`ui-six-fixes-20260909/isolated-subset.log/json`绑定候选树`67370ad29c786ba103ebda674a565298c7ddf6e4`及源码摘要。仅仓库跟踪源码，不含本机第三方研究目录/权重；不是原工作区全套或remote CI，树不含随后追加的本节文档。 |
| 来源守卫与重新连接 | `ui-global-audit-20260909/compare-guards-round5.json`：volume对象/UID变化不恢复旧camera/MPR，用户splitter比例精确恢复。`offline_ui_repair_after.json`：6项正向检查通过，含离线标注保留、重新连接和英文离线状态。 |
| 静态检查 | `ruff check .`、`git diff --check`通过。 |

限制：Qt中心恢复允许约1视口像素取整；未穷举所有窗口/系统缩放、未量化逐帧闪烁、未重跑真实MRI/全部真实数据层，不宣称全局无其他缺陷。下一步用户查看本地候选。

### 历史全局排查：6项新问题的修复前证据

2026-09-09用户要求“全局检查查找类似问题”。主代理检查任务页/侧栏/语言/缩放显示，视觉代理检查对比往返，工作流代理检查真实临时离线工程接入。此轮未修改产品或测试源码；只追加本记录和忽略目录中的审查证据，不将此前回归全绿视为全局无缺陷。

| 优先级 | 复现路径及实际问题 | 位置 |
|---|---|---|
| P1 | 退出对比后V2已恢复主序列冠状图，标题仍写“既往”及上一对比差值。 | `compare_lab.py::_exit_compare_mode` |
| P1 | V2先选冠状面再进入对比，实际绘制既往轴位图，禁用的方向下拉仍显示冠状面。 | `compare_lab.py::_render_compare`、`main.py::_sync_view_controls` |
| P1 | 已有AI结果后打开离线工程，volume_hu已为空，旧“检出1个器官/肝”仍显示，CSV和三维按钮仍启用。仅确证误导显示/按钮状态，未声称实际导出错误数据。 | `annotation_lab.py::open_project`离线分支 |
| P2 | 对比往返丢冠状视图缩放/中心，MPR从开变关；经重建返回也无法恢复进入对比前状态。 | `compare_lab.py::_enter_compare_mode/_exit_compare_mode` |
| P2 | 重建载入模体后打开离线工程，模体对象及生成按钮/标题仍在，但V1被清成纯黑，且未显示离线来源引导。 | `annotation_lab.py::open_project`离线分支 |
| P2 | 真实Ctrl+滚轮使m11从3.85变4.4275，左下角Zoom仍385%，说明缓存没有随视图变换同步刷新。 | `graphics_view.py::wheelEvent`、`main.py::_apply_dicom_overlay` |

证据：`Annotation_Projects/ui-global-audit-20260909/`。`main_probe.py/log/json`在Cocoa实测24个任务页/侧栏/中英文往返组合（1600/1024px）均保持几何/对应状态，并单独记录上述Zoom残留。`compare_probe.py`及`compare-baseline.json`是12×64×64合成CT的offscreen对比往返证据，保存各阶段图像尺寸、标题、控件、camera/MPR及前后源码哈希。`offline_ui_probe.py/json`通过真实临时.miwproj保存/读取复现两项离线问题，AI仅使用合成完成回调、无模型推理。三个诊断脚本exit 0表示成功捕获状态，不代表所测UI行为正确；源码摘要绑定第四轮未提交候选。

检查边界：有代码路径扫描和上述定向交互，不是所有功能、所有数据、所有OS缩放的穷举，完整性不可证明；这轮未重跑全套，也未修改已发现问题。建议先处理来源/方向/结果文字一致性，再处理对比浏览状态、离线模体画面与Zoom即时反馈。

2026-09-09：用户用两张切换前后截图指出工具条高度收缩和纯黑空窗；第三轮保留浏览状态的测试不等于框架几何稳定验收。本轮按用户确认修复此缺口，改动未提交、未push。

- `AdaptiveViewToolbar`按完整控件尺寸计算预留高度及换行，隐藏阅片控件不再压低工具条；窗口缩放仍允许一/两行自适应。四窗进入重建保留已有splitter比例，返回恢复进入前比例。
- V2/V3/V4提供居中、双语的用途/等待原因/下一步提示；使用viewport子QLabel，不加入影像scene、不改sceneRect或相机、不拦截鼠标。真实输出出现即隐藏，换源失效后恢复，返回临床清除。
- 本轮只改`ui_builder.py`、`graphics_view.py`、`recon_lab.py`、`main.py`及测试；不改算法、影像数据或模型。工作流子代理负责空态与分隔比例，主代理负责工具条、集成、测试与本记录。

证据位于`Annotation_Projects/ui-stable-frame-20260909/`（本地忽略目录）：

| 验证 | 结果 |
|---|---|
| 修改前几何复现 | `baseline.log/json`：1600px工具条36→28px；1024px工具条72→28px，边界不变断言exit 1。 |
| 最终原生Cocoa | `native.log/json`：115/115，exit 0，绑定最终源码SHA-256。22项新检查覆盖中英文/宽窄窗的四窗实际viewport位置尺寸完全相同、用户分隔比例、空态生命周期、鼠标穿透；其余为上轮浏览状态/工程/公开CT三面编辑保存Undo回归。无AI推理。 |
| 隔离源码副本 | `SKIP_REAL_DATA=1 python tests/test_gui.py`：1417/1417，exit 0，候选树`a29fca94d16436fac1ac8368ea5fb95720b9359a`。`isolated-subset.log/json`绑定摘要；此树不含随后追加的本节记录，不是remote CI。 |
| 原生视觉复核 | `visual.json`和`clinical-1280.png`/`recon-1280.png`、`clinical-1024.png`/`recon-1024.png`：公开RIDER CT，1280px四窗工具条均36px、1024px均72px；各窗viewport原点和宽高在切换前后精确一致。已查看窄窗截图，文字居中且未覆盖真实影像。 |
| 静态检查 | `ruff check .`和`git diff --check`通过。 |

限制：验证的是指定窗口尺寸下的稳定几何和提示状态，不宣称逐帧闪烁/所有系统缩放均已量化。未重跑真实MRI、全量真实数据层或外部模型。下一步由用户查看本轮切换效果。

## 第三轮实施与本地提交

2026-09-09：用户运行第二轮后认可，并明确下一轮聚焦功能/功能区“切换前→切换后→切回”的画面与状态。随后用户要求“GIT，然后打开我看看”：第二、三轮合并为本地commit `4720b3f`。提交前核对源码SHA-256与以下最终原生/隔离回归证据一致，ruff与diff检查通过；未push。以下各轮“未提交”描述保留为当时实施快照，当前本地状态以本段为准。

第三轮修改 `main.py`、`graphics_view.py`、`recon_lab.py`、`tests/test_gui.py`，本记录由主代理统一维护：

- 分窗显隐、尺寸和补画在同一绘制事务内完成，取消旧布局延迟回调；用户放大不再被强制fit覆盖。Qt `AnchorViewCenter`保留分窗/侧栏显隐时的观察中心；隐藏的各向异性MPR重新补图后恢复既有相机。
- 临床↔重建保存各自的缩放、观察中心与渲染设置，临床相机按来源/序列/平面匹配恢复。临时切页不把MIP重置成单层。进入重建显式清除临床十字线和标注图元，返回后按临床状态重绘。
- 重建输出的pixmap、标题、按钮、耗时与相机按来源对象/序列/层号缓存；模体独立按对象绑定，临床翻层不使模体结果失效。更换真实来源/切片/模体仍作废旧结果并清除耗时。返回重建时V1更新为当前WW/WL，等待提示使用当前语言，已计算结果文案保留运行时语言。
- 重建延迟fit核对模式、来源和切换代次；相机恢复同步完成，避免切回后覆盖新缩放。曾采用“同步恢复再延迟恢复”的中间方案，被快速切换测试准确抓到覆盖新缩放，已移除第二次恢复。

证据目录：`Annotation_Projects/ui-transitions-20260909/`（本地忽略目录）。

| 验证 | 结果与边界 |
|---|---|
| 修改前复现 | `baseline.json`：放大m11从42.5625变6.734375，重建往返投影index从1变0；诊断探针exit 0表示完成记录，不表示行为通过。独立合成探针另复现观察中心漂移、十字线残留及旧耗时。 |
| 已知坏对照 | 仅测试进程恢复布局强制fit，`known-bad-layout.log`的当时21项检查中13项失败、exit 1；证明缩放/中心往返回归能拒绝旧行为。未修改磁盘产品代码。 |
| 最终原生Cocoa | `native-final.log/json`：93/93，exit 0；含23项新增切换回归、已有窄窗/任务页/空态/序列对比/布局补图及公开RIDER CT三面标注保存Undo回归。32²解析模体FBP用于检验结果保留，未运行AI模型。JSON绑定最终源码SHA-256。 |
| 最终隔离副本 | `SKIP_REAL_DATA=1 python tests/test_gui.py`：1395/1395，exit 0；`isolated-subset.log/json`绑定候选树`6ea14c06dc195f1b31777748b05e76b89d48a4cc`及源码摘要。只包含仓库跟踪文件，无本机第三方研究目录/权重；不是远端CI，树不包含随后追加的本节记录。 |
| 原生画面 | `clinical-before.png`、`recon-enter.png`、`clinical-return.png`及`visual.json`：1280×800公开CT，V1变换保留，观察中心允许Qt约1屏幕像素取整；图片拍摄后仅追加隐藏MPR补图修正，该路径另经最终原生回归。 |
| 独立复核 | 工作流代理复现并修正重建缓存/相机；视觉代理复现布局中心漂移和十字线，复测`AnchorViewCenter`后往返中心无累计漂移，单次约1像素取整。 |
| 静态检查 | `ruff check .`、`git diff --check`通过。 |

限制：未量化逐帧闪烁/帧耗时，不宣称所有切换无闪烁；未重跑真实MRI或全部真实数据层，未改算法、模型、数据与实验产物。下一步由用户验收切换行为。

## 第二轮实施与用户验收

用户指令：“先git，然后继续下一轮”。本地提交分开保留依赖与UI边界：

- `fe37ed6a72bbd8e52633db1dae71a83d059e0624`：第一轮UI依赖的CT/MRI多序列、图层、工程保存和Undo基础功能。精确暂存树隔离验证1323/1323，exit 0。
- `574e9414233d02f26b2c76658e9923b3a397aaf7`：用户认可的第一轮UI。精确暂存树隔离验证1348/1348，exit 0。提交后工作区曾核实干净；未push，未声明远端CI。

第二轮只做窄屏适配，产品代码在 `ui_builder.py`、`main.py`、`style.qss`，回归在 `tests/test_gui.py`；另更新本记录和标注计划中的本地提交状态，尚未提交第二轮。

- `AdaptiveViewToolbar` 根据各视图可用宽度将同一批控件重排为一行或两行，不重建控件、不改平面/窗位/投影/厚度值。窄窗仍保留六个可见控件，重建模式隐藏阅片控件后标题占整行。
- 左下角提供“收起面板 / 展开面板”，按钮一直可达；可用标准焦点加Space操作。曾尝试的Ctrl+Shift+P在本机Qt事件测试中未触发，已移除该全局快捷键及其提示，不保留未经验证的入口。
- 初始化先让所有控件进入工具条父布局，再进行显隐和重排。独立复核发现的无父控件问题已实测复现（修复前六项父级状态为1真5假），修正后补齐独立工具条和空载首次显示回归。

第二轮证据：`Annotation_Projects/ui-secondpass-20260909/`（忽略的本地证据目录）。

| 验证 | 结果 |
|---|---|
| 修改前窄窗复现 | offscreen请求1024×768，实际1228×768；目标宽度断言exit 1，`baseline-narrow.json`。 |
| 原生Cocoa回归 | 63/63，exit 0，`native.log/json`。包括24项窄屏控件/点击/状态保持检查，以及第一轮工程保存恢复、原位语义、原始AI显示与工作统计来源、公开CT三面编辑和Undo回归；未启动整卷器官推理。 |
| 原生窗口尺寸 | 右栏显示时实际1024×768；右栏收起时实际900×768；有中英文截图 `native-narrow.png`、`native-narrow-en.png`、`native-collapsed.png`、`native-collapsed-900.png`。 |
| 隔离源码副本 | `SKIP_REAL_DATA=1 python tests/test_gui.py`：1372/1372，exit 0。候选树 `f2aac70a2ecf5a5efa652d79b8bae06dd9980cbc`，`isolated-subset.log/json`绑定源码SHA-256。该树验证时尚未追加本节状态记录；不是已commit或远端CI。 |
| 静态检查 | `ruff check .`、`git diff --check`通过。 |

隔离副本只含仓库源码/已有跟踪文件，不含本机研究目录和外部数据/权重；因此与原工作区1366/1367的口径不同，不宣称修复了原第三方Markdown扫描问题。尚未覆盖系统大字体、所有屏幕尺度或真实MRI。用户已运行并认可第二轮；后台导入与AI运行控制仍不在该轮范围。

## 第一轮实施与用户验收

授权：用户在多角色审查后回复“可以”，同意第一轮常驻保存反馈、明确操作目标/结果来源、侧栏分组和按钮统一。

- 顶部并列打开/保存工程，保存状态常驻；失败原因继续由既有保存机制提供。光标读数固定在侧栏底部。
- 阅片侧栏分成“阅片 / 标注 / 结果 / 序列”四页，仅组织控件；选用分割画笔/橡皮时显示参数页，不修改文档或重置历史。
- 清空入口移到标注页，显式选V1–V4目标视图，显示平面与图层；隐藏、只读或不可编辑目标禁用。底层清空/Undo契约不变。无解剖几何时写“原始体素平面”。
- 结果页分别注明画面来源与统计/CSV/三维来源；只读原始AI与当前工作结果的区别保留。
- 所有按钮有统一暗色基础样式及焦点/禁用状态；提高说明文字可读性，器官图例增加键盘链接访问。
- 中央空态提供导入/打开工程入口；离线工程明确标注已保留、等待连接匹配DICOM。重建模体在重建模式正确显示，跨Tab/卸下恢复对应状态。
- 同时修正空载“1 / 0”层号与空白WW/WL标签，以及语言切换后常驻HUD残留中文。

本轮修改：`ui_builder.py`、`style.qss`、`main.py`、`annotation_lab.py`、`recon_lab.py`、`tests/test_gui.py`及本记录。用户已查看运行界面并认可此版。其依赖的CT/MRI基础功能已单独保存为本地commit `fe37ed6a72bbd8e52633db1dae71a83d059e0624`；本记录随UI快照提交，不包含push或远端CI声明。

验证证据位于 `Annotation_Projects/ui-firstpass-20260909/`（本地忽略目录，非远端CI）：

| 验证 | 最终结果与边界 |
|---|---|
| 原生Qt Cocoa，`test_ui_task_workflow`、`test_ui_empty_and_source_states`、`test_document_ai_lifecycle`、`test_real_ct_annotation_project` | 39/39，exit 0。包含公开CT三面鼠标标注、保存、重开、Ctrl+Z与模体FBP；未启动整卷器官推理。`native-final.log/json`绑定最终源码SHA-256。AI生命周期测试使用合成回调结果，不是模型性能验证。 |
| `SKIP_REAL_DATA=1 python tests/test_gui.py` | 1366/1367，exit 1。唯一失败为未改动的Markdown检查扫描到了研究目录中Detectron2第三方文档的星号文本；不可称全绿。`subset-final.log/json`记录源码摘要，运行前后源码一致。 |
| Markdown失败归因 | 修改前备份与当前该检查函数逐字一致；单独运行再次得到同一第三方文档失败。`baseline-markdown-check.log/json`。未删除外部文档或放宽检查。 |
| 已知坏清空绑定 | 仅在测试进程中把按钮变回不传vid的旧调用；14项中准确只有“清V1而非上次V2”失败，exit 1。`known-bad-clear.log/json`；没有修改磁盘产品代码。 |
| 静态检查 | `ruff check .`、`git diff --check`通过。 |
| 独立复核 | 视觉角色提出的空载标签/双语HUD问题已修；工作流角色发现的模体空态/原位方向问题已修并经源码复核关闭。 |

窗口覆盖包括1280×800原生单窗/四窗、中英文和四任务页。1024×768四窗请求在Cocoa仍被撑至1211×768：本轮没有实现窄屏自适应，不将其列为已解决。系统大字体、屏幕阅读器、真实MRI和全量真实数据测试未重跑。DICOM后台导入与AI显式运行控制留待后续，未改变算法或模型准入。

下一步：用户已认可第一轮并要求先git再继续，后续做视图工具条与侧栏窄屏适配。原始审查问题与证据保留如下，不将已实施项目误认作仍未动工。

## 初次审查结论

优先改进操作对象、结果来源、保存反馈和恢复引导，再整理侧栏及视觉一致性。已有影像中心布局、Undo、只读 AI 和工程恢复机制值得保留。本审查不等同功能全量验收、临床验证或无障碍认证。

方法：三名独立子代理（visual_review：视觉布局；workflow_review：阅片/标注工作流；accessibility_review：可用性与边缘状态），主代理核对源码并补当前 Qt 离屏渲染。视觉角色和技术角色独立评估后合并，未用旧截图代替当前实现。

## 优先问题

P1 表示优先处理的操作歧义或反馈问题，不表示已观察到数据损坏；P2 表示效率、可读性和一致性改进。

| 优先级 | 问题与依据 | 建议及验证边界 |
|---|---|---|
| P1 | “清空当前面”采用 `_last_edit_vid`，由编辑上下文捕获更新，而非显式指定当前目标。`annotation_lab.py:301,983`，`ui_builder.py:426`。 | 按钮注明平面、层号、图层，或移入各视图局部菜单。源码证实目标规则；用户误清场景尚未做交互复现。 |
| P1 | 原始 AI 对照只改变显示层，统计仍为工作结果；提示写“下方统计”，实际统计在图层控件上方。`annotation_lab.py:377,467`，`ui_builder.py:310,357`。 | 图层选择提前，统计/CSV/三维按钮显式注明结果来源。源码证实，未运行真实 AI。 |
| P1 | 保存状态和打开工程在长滚动区底部，清空操作常驻。`ui_builder.py:397,421`。 | 顶部固定保存状态与打开工程；清空降为局部次级操作。1280×800 CT 离屏图确认保存状态需滚动才能看到。 |
| P1 | 未连接 DICOM 时打开工程清空影像，仅有 offline 序列和 tooltip 解释。`annotation_lab.py:1372,1465`，`main.py:794`。 | 空态明确“工程已打开，标注已保留，请连接原始 DICOM”，提供直接入口。源码证实空影像路径；用户误判保存失败是交互风险。 |
| P2 | 新增配准、病灶、Undo、工程管理、清空当前面按钮没有沿用 ActionBtn 样式。`ui_builder.py:364,399,426`，`style.qss:25`。 | 统一按钮默认/悬停/焦点/禁用/选中状态。当前 offscreen 截图确认白色原生按钮与暗色按钮混杂；原生 cocoa 仍待检查。 |
| P2 | 阅片上部排列参考序列及配准，下部串联 AI 定量、图层、随访、保存等功能。`ui_builder.py:183` 起。 | 按“阅片 / 标注 / AI 结果 / 序列配准”渐进展开，当前工具相关内容优先；保存状态常驻。截图确认高频窗位预设已接近首屏下缘。 |
| P2 | 固定侧栏与视图工具条限制窄窗。`ui_builder.py:74,106,579`，`style.qss:47`。 | 支持侧栏折叠/调整宽度、低频视图操作收进菜单。加载 CT、四窗英文模式请求1024×768，实际得到1228×768；不是已验证所有屏幕尺寸。 |
| P2 | 小字提示低对比：`#5C677D` / `#1C1F26` 为2.90:1；10px免责声明 `#C0392B` / 同背景为3.03:1。`ui_builder.py:229,329`。 | 提高说明字的字号和对比度，风险颜色辅助强调。数值是源码颜色的 sRGB 计算，不是显示器测量。 |
| P2 | DICOM 在主线程同步读取解码，没有该阶段进度/取消；多检查选择仅列UID。`main.py:744,752,1538`，`study_data.py:335`。 | 后台候选解码、阶段进度/取消，检查列表展示可读描述。同步调用已证实，未测大型目录阻塞时长。 |
| P2 | 器官图例限定 `LinksAccessibleByMouse`。`ui_builder.py:305`。 | 可聚焦复选列表或增加键盘访问并验证焦点；本轮未做屏幕阅读器/键盘全链路验收。 |
| P2 | 合资格 CT 可自动启动 AI，手工编辑会停止；缺少显式运行/停止/重试入口。`main.py:905,1081`，`ui_builder.py:293`。 | 增加用户可见的 AI 运行控制，并区分当前模态能力。属于交互设计建议，不应以此绕过现有模型和来源准入。 |

补充视觉意见：实时光标/强度读数位于滚动内容末端（`ui_builder.py:411`），应纳入固定状态条；左侧九个同形双行文字工具缺少导航/测量/像素编辑分组（`ui_builder.py:69`），可引入统一图标并保留文字标签，优先级低于状态歧义。首次空载中央全黑，可提供加载DICOM/打开工程的就地引导。

其他待测：长患者ID/中英文名称无换行、完整值提示或复制策略（`ui_builder.py:175`）；重建按钮饱和色及禁用态的视觉一致性；系统字体放大、原生 macOS 焦点和快捷键。未将待测项报告为已复现缺陷。

## 初次审查时的渲染与限制

- 环境：conda `dicom_gui`，`QT_QPA_PLATFORM=offscreen`，macOS。
- 两次临时 Python 探针退出码均为0。直接构造 `MedicalViewer(project_dir=<临时目录>, autosave=False)`；第二次将 `_kickoff_ai` 替换为空函数，仅用于防止审查触发推理，将 persistence/export 路径指向临时目录后调用 `load_data('肺癌')`。
- 使用仓库现有公开去标识 RIDER CT，只读取数据，未保存项目或运行 AI/重建计算。该运行不能验证正常 AI 生命周期或自动保存。
- 渲染覆盖：空载1600×950、空载四窗1280×800中文/英文、CT四窗1280×800、CT侧栏底部、重建Tab、CT四窗英文1024×768请求。
- 临时截图：`/var/folders/9h/ls0pl_t51sncg62_31v0n7l40000gn/T/gui-ui-review-20260909-onsxcxvs/`。临时目录可能被系统清理；关键结果已记入本文件。
- Impeccable detector 命令：`node /Users/sc/.codex/plugins/cache/impeccable/impeccable/3.9.1/skills/impeccable/scripts/detect.mjs --json /Users/sc/01_Projects/GUI/ui_builder.py`。退出码0，输出`[]`。HTML规则不适用于此Qt目标，不代表UI无问题。没有浏览器页面、注入或可视化覆盖层。
- 未重跑功能全套、真实 MRI、真实 AI、保存恢复、清空/Undo交互或原生 cocoa。本轮不输出整体完成百分比或未经完整量表测试的健康评分。

审查时源码SHA-256：

```text
f24330c06ea6eccc7814dc788dad1aa9ce31478b7d87e27aa8bd17a448c81e76  main.py
bf0832032689df1ba82dc5245f2f68033b5311ef023bad25be733b4dc64f4b2a  ui_builder.py
ff8fd21fa6ca548094c16abf577e7396d2b9af640fba2814e833eac2c52b4afc  style.qss
faa5823804a4d6cc19200e72a579050ab61e996e8de0727f1e35de46efb096ce  annotation_lab.py
9d20393181d807361c4254b4f45e2ecae04858013250f2fe845f8d71925633e0  study_data.py
```

## 初次审查时的建议

审查阶段建议先确定侧栏和常驻状态区的布局方案；首批同时解决目标面、结果来源、保存反馈、工程重连引导。第二批统一按钮、文字和窄屏布局。后台导入与AI控制单独实施，保留已有数据/编辑/保存契约并作针对性验证。当时仅写入审查记录；后续用户授权的第一轮实施与验证见本文顶部。

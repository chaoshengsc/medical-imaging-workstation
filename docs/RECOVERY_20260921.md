# 2026-09-21 本机恢复记录

原工作目录 `/Users/sc/01_Projects/GUI` 已无法访问。该目录最后一次可验证的
本地提交为 `068c2b31f6a597edce2ec39a4a43fcad2133b37b`；它没有出现在远端仓库。

本目录从远端 `main` 的 `fa2c8bbf401e4ce0eec446a585368114e37a3b5e` 重建。下列
未推送源码从本机 Codex 会话中记录的命令标准输出逐段恢复，并经过本地语法与单元测试：

- `tumor_model_admission.py`：候选模型证据卡、工程/产品/逐病灶类型三层准入和 VS T1
  静态空间身份检查。它不会授权模型执行。
- `tests/test_tumor_model_admission.py`：6 项 fail-closed 回归测试。

未恢复的内容包括 `Annotation_Projects/` 下的病例、DICOM、mask、模型权重、独立运行环境，
以及缺少完整源码的 AI 面板、缓存和预处理适配层。它们未被视为已恢复，也没有从网络下载替代品。

验证命令：

```bash
cd /Users/sc/01_Projects/MUI
python3 -m py_compile tumor_model_admission.py
python3 -m unittest tests.test_tumor_model_admission -v
git diff --check
```
